import com.mojang.serialization.Codec;

public interface cut<P extends cus> {
	cut<cur> a = a("always_true", cur.a);
	cut<cup> b = a("linear_pos", cup.a);
	cut<cuf> c = a("axis_aligned_linear_pos", cuf.a);

	Codec<P> codec();

	static <P extends cus> cut<P> a(String string, Codec<P> codec) {
		return gl.a(gl.aJ, string, () -> codec);
	}
}
