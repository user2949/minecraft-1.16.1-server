import com.mojang.serialization.Codec;
import java.util.Random;
import java.util.stream.Stream;

public class cso extends csq<cnz> {
	public cso(Codec<cnz> codec) {
		super(codec);
	}

	public Stream<fu> a(Random random, cnz cnz, fu fu) {
		return Stream.of(fu);
	}
}
