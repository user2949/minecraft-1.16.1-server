import com.mojang.serialization.Codec;
import java.util.Random;

public abstract class cpo {
	public static final Codec<cpo> a = gl.at.dispatch(cpo::a, cpp::a);

	protected abstract cpp<?> a();

	public abstract cfj a(Random random, fu fu);
}
