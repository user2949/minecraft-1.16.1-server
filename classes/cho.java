import java.util.function.Predicate;
import javax.annotation.Nullable;

public interface cho<T> {
	int a(T object);

	boolean a(Predicate<T> predicate);

	@Nullable
	T a(int integer);

	void b(mg mg);

	int a();

	void a(lk lk);
}
