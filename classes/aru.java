import com.google.common.collect.ImmutableMap;
import com.google.common.collect.ImmutableSet;
import com.mojang.datafixers.util.Pair;
import java.util.List;
import java.util.Map;

public class aru<E extends aoy> extends aqr<E> {
	public aru(List<Pair<aqh<? super E>, Integer>> list) {
		this(ImmutableMap.of(), list);
	}

	public aru(Map<awp<?>, awq> map, List<Pair<aqh<? super E>, Integer>> list) {
		super(map, ImmutableSet.of(), aqr.a.SHUFFLED, aqr.b.RUN_ONE, list);
	}
}
