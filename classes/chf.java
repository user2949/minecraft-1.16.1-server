import it.unimi.dsi.fastutil.longs.LongSet;
import java.util.Map;
import javax.annotation.Nullable;

public interface chf {
	@Nullable
	ctz<?> a(cml<?> cml);

	void a(cml<?> cml, ctz<?> ctz);

	LongSet b(cml<?> cml);

	void a(cml<?> cml, long long2);

	Map<cml<?>, LongSet> v();

	void b(Map<cml<?>, LongSet> map);
}
