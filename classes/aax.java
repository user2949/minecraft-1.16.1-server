import java.util.concurrent.CompletableFuture;
import java.util.concurrent.Executor;

public interface aax {
	CompletableFuture<Void> a(aax.a a, abc abc, ami ami3, ami ami4, Executor executor5, Executor executor6);

	default String c() {
		return this.getClass().getSimpleName();
	}

	public interface a {
		<T> CompletableFuture<T> a(T object);
	}
}
