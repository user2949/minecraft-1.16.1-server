import java.util.Random;
import javax.annotation.Nullable;

public class cet extends ces {
	@Nullable
	@Override
	protected ckb<cou, ?> a(Random random, boolean boolean2) {
		return ckt.c.b(brf.N);
	}
}
